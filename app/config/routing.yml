app:
    resource: '@AppBundle/Controller/'
    type: annotation
    prefix:   /
# redirecting the root
root:
    path: /
    defaults:
        _controller: FrameworkBundle:Redirect:urlRedirect
        path: /femmes
        permanent: true
